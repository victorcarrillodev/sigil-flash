{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schema-playlist-staging.json",
  "title": "Staging Playlist",
  "description": "Playlist being downloaded to staging/ (includes download progress)",
  "type": "object",
  "required": ["_schema_version", "playlist_id", "version_hash"],
  "properties": {
    "_schema_version": { "enum": ["1.0", "2.0"] },
    "playlist_id": { "type": ["string", "null"] },
    "playlist_version": { "type": "integer", "minimum": 0 },
    "version_hash": { "type": ["string", "null"] },
    "source": { "type": "string" },
    "stop_playback": { "type": "boolean" },
    "total_size_bytes": { "type": "integer", "minimum": 0 },
    "created_at": { "type": "string", "format": "date-time" },
    "download_status": {
      "type": "object",
      "properties": {
        "total_tracks": { "type": "integer", "minimum": 0 },
        "downloaded_tracks": { "type": "integer", "minimum": 0 },
        "verified_tracks": { "type": "integer", "minimum": 0 },
        "validated_track_ids": { "type": "array", "items": { "type": "string" } },
        "failed_tracks": { "type": "array", "items": { "type": "string" } },
        "progress_percent": { "type": "integer", "minimum": 0, "maximum": 100 },
        "started_at": { "type": "string", "format": "date-time" }
      },
      "additionalProperties": false
    },
    "tracks": {
      "type": "array",
      "minItems": 0,
      "items": {
        "type": "object",
        "required": ["id", "url", "filename", "sha256"],
        "properties": {
          "id": { "type": "string" },
          "url": { "type": "string" },
          "filename": { "type": "string" },
          "sha256": { "type": "string" },
          "size_bytes": { "type": "integer", "minimum": 0 },
          "duration_seconds": { "type": "integer", "minimum": 0 },
          "title": { "type": "string" }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
