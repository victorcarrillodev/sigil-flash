# Sigil OS — required apt packages
# Source: pi-gen/stage-sigil/00-install/00-packages + sigil-hardware/install.sh
#
# This is the canonical manifest. pi-gen and install.sh are legacy references.
# Keep this list minimal: production devices should not include SSH, git, wget
# unless explicitly needed for factory mode.

# --- Core runtime ---
python3
python3-flask
python3-bluez
python3-pip
network-manager
bluez
bluetooth
pulseaudio
pulseaudio-utils
pulseaudio-module-bluetooth
alsa-utils
mpg123

# --- WiFi AP fallback ---
hostapd
dnsmasq
wireless-tools
rfkill
iw

# --- System ---
sudo
curl
raspi-utils

# --- Firmware / wireless ---
firmware-brcm80211
wireless-regdb

# --- Optional (factory/debug, not installed by default) ---
# openssh-server      # only for factory mode, keep disabled
# python3-venv         # if isolated Python env needed
# bluez-tools          # debug only
# jq                   # if provisioning uses JSON in shell
# git                  # not needed in production image
# wget                 # not needed in production image
# python3-qrcode       # removed per tasks.md
# python3-pil          # removed per tasks.md
