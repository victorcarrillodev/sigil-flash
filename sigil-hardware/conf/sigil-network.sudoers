# Network sudo permissions for sigil user — ZERO entries remain.
#
# All wlan0 mutations (scan, connect, AP recovery, driver config, NM profile
# management) are exclusively owned by sigil-wifi-control.service (root).
# The panel communicates via Unix socket — no sudo required.
#
# Bluetooth sudo permissions are in sigil.sudoers.
