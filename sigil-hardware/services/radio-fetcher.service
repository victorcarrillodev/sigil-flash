[Unit]
Description=Sigil Radio Fetcher — playlist download, hash verification, atomic swap
After=sigil-local-bootstrap.service network.target audio-manager.service
Wants=sigil-local-bootstrap.service
Requires=audio-manager.service
BindsTo=audio-manager.service
StartLimitIntervalSec=60
StartLimitBurst=5

[Service]
Type=simple
User=sigil
Group=sigil
Environment=AUDIO_CONF=/etc/sigil/audio.conf
ExecStart=/usr/local/bin/radio-fetcher.sh
Restart=on-failure
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
